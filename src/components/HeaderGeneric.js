import React from 'react'

const HeaderGeneric = ({title, description}) => (
    <header id="header">
        <h1>{title}</h1>
        <p>{description}</p>
    </header>
)

export default HeaderGeneric
